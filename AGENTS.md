# Project Rules & Context

## Administrative Access
- Full administrative access is strictly limited to **ynn.delro7@gmail.com**.
- All other users are considered standard donors.

## UI/UX Guidelines
- The Foundation Audit Trail in the dashboard should always remain visible with high contrast (Teal text on Dark/Slate background).
- All financial transactions and smart contracts should be searchable via the Transparency ledger.

## Data Invariants
- When registering new boutique assets, always include `createdAt` and `lastUpdated` timestamps.
- Donor reports generated by admins should be added to the persistent reports vault for the current session.
